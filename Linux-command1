pwd -Linux command to print out current working directory
ls -list directories
pwd -print working directory
cd -To navigate through directories.
mkdir -To create directories
mv -Move or rename files.
cp -For copying files in linux
rm -Delete files or directories.
touch -To create blank/empty files.
cat -Diplay files or directories
clear -clear the terminal display
echo - Print any text taht follows the command
less -command to display paged outputs in the terminal.
man -access manual pages for the commands.
uname -commands to get basic information abiut OS.
whoami -Get active username
tar -To extract and compress files 
grep -Search for a string within an output
ssh -secure shell
ps -Display active processes
kill and killall -Kill active processes by ID or name
chomd -Command to change file permissions
chown -Command for granting ownership of files or folders
sudo -Command to escalate privileges
useradd and usermod -Add new user or change existing users data
passwd -Create or update passwords for existing users
Content added from the cloud
